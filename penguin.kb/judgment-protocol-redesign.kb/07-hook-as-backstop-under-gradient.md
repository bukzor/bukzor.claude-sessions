# Does the withdrawn `Stop` hook return as a backstop?

Posed 2026-09-19, unruled. Reopens a withdrawal.

The hook (regex the finished reply for concession openers; block with
"name what changed") was withdrawn 09-19 as a patch on the symptom.
`must-read-sharding`'s ruled ledger frames it differently: GRADIENT --
an occasion moves from judgment-pulled to mechanically-pulled when a
runtime can detect it begin; BACKSTOP -- a mechanical puller anchored
to an action fires after planning, so for an approach-shaping body it
is a backstop under the judgment puller, and "defense in depth is the
floor's own rule, so this costs nothing new."

Under that frame the hook is the prescribed retirement path for the
bank's lowest-NOTICED occasion, subordinate to a judgment puller, not
a substitute. What the frame also says: a fold check is
approach-shaping, so the backstop is late -- it can force a visible
retraction, never prevent the fold.

Proposed standing: pre-commit at assertion (01-) is primary; the hook
is a backstop candidate, ruled separately, after 06- says how much of
the 63 was NOTICED at all. Its detection condition (the regex) is an
instance of what SET_DESC? needs a home for.

Sibling: `trigger-installation.md` reads the same frame as licensing a
`PreToolUse` first-call hook for the bank installer. Different
occasion, same argument; that ruling is theirs to seek.
