# The four replacement rules stand as fleet doctrine

Ruled 2026-09-01: "looks good." All four, unamended, including the
refinement your rename supplied — prose is where a variant waits, not
where it belongs once it turns out to be the primary occasion.

1. **Broaden the canonical slug** until it names the family — but only
   while it stays concrete.
2. **Keep the name that fires** — a proven slug outranks the incumbent.
3. **Name the remaining occasions in the opening prose.**
4. **No honest broader name means two entries**, sharing a
   `procedures.kb/` method.

Live in `llm-must-read-kb/SKILL.md`, Step 7 of
`references/create-new-trigger.md`, and
`docs/dev/adr/2026-08-29-001-No-aliasing--one-body-gets-one-filename.md`.
The ADR's status line still marks them agent-authored and vetoable;
that mark is now stale and should read as ruled.
