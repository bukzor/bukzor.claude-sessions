# 2026-09-19: fourth peer — shell loader as GRADIENT's endpoint; REENTRY

Source: `../shell-config-intent-first-loader.kb/2026-09-19-001-sibling-review.md`
(session behind `shell-config-intent-first-loader.md`), pointed at by a
peer message 14:16; verified on disk. It imported WHILE_KEY (as
DIR_IS_INTENT), NAMESPACE (as per-class), and restated its AXIS rule
against SHAPE; it offered two claims back. Filed here as its author's,
not adopted as rulings.

```
* FIRING+
  * REENTRY+ <- ROOT COMPACT_REINSTALL: the top set's installer occasion
    is "entry into a context window" -- session start and every
    post-compaction resume alike -- and installing is idempotent, so
    re-running it on every entry costs one listing and closes COMPACTION
    for the top set without tracking state. Offered by the shell-loader
    peer as its own practice (re-run the loader at every entry;
    idempotent legs); here it is the "per context window" reading that
    `trigger-installation.md`'s open item 1 asks for. Stale when: an
    install stops being idempotent (a listing that mutates state).
    * // attributed: shell-config-intent-first-loader, 2026-09-19.
  * MECH_ENDPOINT <- GRADIENT: the shell loader is GRADIENT's endpoint
    running today -- every guard runtime-decided, NOTICED free on every
    node -- and it exists because its conditions are shell-decidable
    predicates. The bank's occasions are mostly judgment-shaped; the
    endpoint is reachable per occasion exactly as far as SET_DESC? gets
    a decidable condition to hold. No change to GRADIENT; a worked
    instance of where it ends.

* GRAMMAR+
  * RESERVED (note): the peer's AXIS restatement sharpens why the four
    junctures are directories -- a reserved name is a directory iff it
    contains the open set (trigger files under before/after/when, sets
    under while/). Consistent with SHAPE; recorded as the shorter
    statement.
```

Not transferable: the peer's subtractive fix (every directory is an
intent; no reserved name) works because intent.d has no juncture axis.
The bank's plain directories under a point juncture are prefixes, not
sets, so `while/` stays.
