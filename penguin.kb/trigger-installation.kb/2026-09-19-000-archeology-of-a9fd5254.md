# 2026-09-19: Archeology of a9fd5254 — the static trigger that never fired

Transcript: `~/.claude/projects/-home-bukzor/a9fd5254-2b3d-4e9e-9c19-9ec328b2a766.jsonl`
(1273 records at time of reading). Line numbers below are record lines in
that file. Read via `claude_code_archeology`-style JSON decoding, never raw
grep (`Skill(claude-code-archeology)`).

## Chain of events, first turn

- `L5` user, 250 chars: the `/incident-forensics` command wrapper.
- `L6` user, 5257 chars, parent L5: the skill body, opening
  `Base directory for this skill: … --- triggers: - read: - Skill(llm-kb) -
  Skill(llm-subtask) - Skill(upstream-reporting) - Skill(walled-web) ---`.
  User role, last text in context when generation begins.
- `L13` attachment `instructions`: `/home/bukzor/.claude/CLAUDE.md` (5878
  chars, User) containing `must-read://~` and `before: your first tool call`
  verbatim; `/home/bukzor/CLAUDE.md` (72 chars, Project).
- `L17` attachment `prompt_snapshot`: harness system prompt, 14570 chars.
  Contains neither `must-read` nor `llm-must-read-ls`. Contains: *"Before
  your first tool call, state in one sentence what you're about to do."*
- `L22` assistant thinking (2026-09-16T18:42:49Z), model `claude-sonnet-5`:
  *"I should start by checking the frontmatter triggers on the
  incident-forensics skill, which reference reading several other skills…
  These read directives seem unconditional since they're just listed without
  a separate occasion, so I'll load them all now. My plan: load llm-kb and
  llm-subtask… then investigate the actual incident by reading the cron
  log…"* No mention of CLAUDE.md, step 0, or `must-read://~`.
- `L23` **first tool call**: `Bash(cat …/pnpm-upgrade-g.log | tail -200)`.
  `L24` `Skill(llm-kb)`, `L25` `Skill(llm-subtask)` -- one parallel block. No
  text block precedes them (harness one-sentence rule also unmet).
- `L39-L42`: second turn loads `upstream-reporting`, `walled-web`; `L42` is
  the first git command (`cd … && git log --oneline`, non-compliant form).

## Never recovered

Compaction boundaries at `L394` (09-16T18:58Z), `L862` (09-17T20:26Z),
`L1157` (09-18T15:21Z). First tool call after each: `Read` of a findings
file; `Bash grep/ls`; `Bash grep` on this transcript. No listing run.

All `must-read` access in 131 Bash calls: `L1032` `find … -iname '*git*'`
and `L1036`/`L1038` `Read` of `before/git/running-ANY-git-command.md`,
`commit.md` -- in response to the user's *"you didn't read the
must-read/git?"*. The llm-kb skill's own IMPERATIVE (`ls -RF
skill.kb/must-read.kb/` as first action) was likewise never run.

## Fire-rate measurement

Method: for every `~/.claude/projects/*/*.jsonl` modified in the last 45
days, take the `instructions` attachment's `~/.claude/CLAUDE.md` content
and classify its wording (`must-read://~` present → new; `ls -RF
~/.claude/must-read.kb` → old; else other), the first user text (starts with
`<command-message>` → skill-first; starts with "This session is being
continued" → excluded), the first assistant `model`, and the ordinal of the
first Bash call whose command contains `llm-must-read-ls` or `must-read.kb`.

Result (fired at call #1 / sessions):

| wording | prompt      | fable | opus         | sonnet | haiku |
| ------- | ----------- | ----- | ------------ | ------ | ----- |
| old     | plain       | 5/5   | 15/15        | 2/2    | 1/1   |
| old     | skill-first | 6/6   | 1/1          | --     | --    |
| new     | plain       | 19/19 | 12/14 (2 @#2-5) | 5/7 | --    |
| new     | skill-first | --    | --           | 0/2    | --    |
| other   | plain       | 0/1   | 1/1          | 8/25   | 0/27  |

"other" is mostly a wording that predates both, plus haiku sub-sessions;
haiku 0/27 is a separate fact about haiku, not about wording.

The two new/skill-first failures: this session and `923c142e`
(`/session-start`, sonnet; first calls `date -Is`, `cd … && git log`,
`find`). A first pass of this measurement mis-searched the old cohort and
reported 9/54 for it; that figure was retracted in-session.

## What changed on 2026-09-04 (`4614693`, dotfiles)

Removed: `> IMPERATIVE: Your FIRST tool use in every conversation MUST be:
Bash("ls -RF ~/.claude/must-read.kb")`; *"While planning, before taking ANY
action, review **all three** trigger banks"*; `> WARNING: You WILL FAIL…`.
Added: Definitions (occasion / trigger / installed / `before` /
`must-read://DIR`), and an "Installation" list whose item `0. statically:
before: your first tool call / read: must-read://~` is the only trigger among
four sources.

## Reading

Not absence, not placement: the trigger was present and CLAUDE.md-resident
wording had been 30/30 before the rewrite. The rewrite traded an
enumeration verb at planning time ("review all banks before acting") for a
standing-state description ("continually monitor") plus a source taxonomy,
and on sonnet with a skill body arriving bare and nearest, the one
planning-time retrieval went to the skill's triggers and stopped.
