---
cwd: /home/bukzor/repo/github.com/bukzor/ideation.epistemics/session-model
session:
  uuid:
    - f9bdf6f0-d5fd-48a2-b065-da485f78241c
  started: 2026-09-09T11:54:22-05:00
  ended: null
---
# Session Model: Build Budget and Trace-Sim, Rule the Carrier Contention

The session-model ledger (`session-model.claims.md` in the cwd, with its
`commitment`/`ask` priors in bukzor-agent-skills' `design.claims.kb/` and
`carriage` in its `strata.claims.kb/`) specifies three simulations at
increasing detail. The first two are buildable now and each has a stated
exit. One stipulation is waiting on the owner before the strata ledger is
clean. Read `claude-code-holistics/review.kb/2026-09-09-what-licenses-a-law.md`
first; its re-entry path covers the sitting that produced all of this.

- [ ] Rule the `carrier` contention in `strata.claims.kb/`: `carriage`
      (the owner's sense) versus `data-structures` (the algebraic carrier
      set). Recommendation on file in the 2026-09-11 devlog: rename the
      data-structures word to `carrier set`.
- [ ] Build `budget` (`session-model.claims.kb/budget.kb/`): tokens per
      load times loads per week over the instruction layer, from file
      sizes and tier tags. Exit: reproduce the 88% delegation measurement
      in `authorship.kb/price-text-by-load-frequency.md`.
- [ ] Build `trace-sim` (`trace-sim.kb/`): event traces, carriers loading
      by tier, a recognizer parameter per directive, three oracles. Exit:
      reproduce the acts-not-laws case and price a write-directory hook's
      false fires.
- [ ] Close the two open runtime claims in `strata.claims.kb/claude-code.kb/`
      with their named checks: whether CLAUDE.md survives a compaction
      boundary, and which tool events a hook can intercept.
