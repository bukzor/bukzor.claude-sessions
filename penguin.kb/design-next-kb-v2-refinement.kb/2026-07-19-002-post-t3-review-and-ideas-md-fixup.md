# 2026-07-19: post-T3 review — ideas.md fixup; goals review opened

Fresh-eyes review of the T3 artifacts (separate session) found the
three recordings disagreeing on the session's central decision: the
ADR's final state (two full synthesis pairs — `todo.md`/`todo.kb/`
and `ideas.md`/`ideas.kb/`; `## Later` retires) versus
`class-task.md` and the T3 brief, which still recorded the
superseded midpoint (`## Later` as contract boundary inside one
working set). Operator confirmed the ADR: `ideas.md` is the
synthesis of `ideas.kb/`, exact analogue of `todo.md` : `todo.kb/`.

- Fixed `class-task.md` ("Two contracts": pair model, filename
  dispatch, pointer-line asymmetry — `todo.md` carries one per open
  entry, `ideas.md` none), the T3 brief's horizon-ratification
  bullet, and `task-synthesis-drift-check.md` (the option pair
  inverts the every-entry-has-a-line rule). Amended into the T3
  commit. Addendum -001's Horizon bullet records the midpoint;
  the ADR + `class-task.md` are the decided state.
- Left generic: class-task's elaboration step 2 ("the line becomes
  the entry's pointer") is obligation-shaped; whether to note the
  option asymmetry there is a queued micro-decision.
- Opened the ecosystem-goals discussion (mission's three jobs:
  durable knowledge / attention / convention enforcement) as prelude
  to assessing v2.2 against them — framing delivered, operator
  reaction pending. Queued in-repo under the green-light gate
  (`.claude/todo.md`), including the open placement of llm-vitals /
  llm-chat-librarian / claude-realignment and the one-operator
  assumption.
