# yaml-language-server: $schema=https://json-schema.org/draft-07/schema
# Per-host indirection: llm.kb-validate resolves `penguin.kb/*.md` against
# `penguin.jsonschema.yaml`, so each host directory needs one of these. The
# schema itself is shared and lives one level up, in dotfiles.
$ref: "../sessions.jsonschema.yaml"
