# 2026-07-19: T3 session — task domain dissolves into spec primitives

T3 settled — all three axes, operator-ratified incrementally, each
recorded before moving on. Journey ADR (derivations, corrections,
rejected alternatives):
`bukzor-agent-skills/docs/dev/adr/2026-07-19-000-Task-domain-dissolves-into-spec-primitives--T3-.md`.

- **Grain**: dissolved via linked-node + synthesis specialization —
  gradual elaboration (line → file → task kb), self-similar task
  tree; store choice carries nothing. Recorded in `class-task.md`.
- **Horizon**: the working set's total order, nothing else. `## Later`
  reframed as the obligation/option *contract boundary* (options =
  line-grain ideas; sweeps enumerate everything, nag only
  obligations). "Likelihood" dissolved into the contract binary +
  ratings-driven reevaluation — operator correction absorbed: wsjf
  originated in ideas.kb as the option pool's refresh mechanism.
- **Scope**: "project" defined (ownership, not git boundaries);
  sessions.kb = the task class instanced at operator scope,
  machine-sharded (working state is per-machine); "supportable" =
  minimal-core-forever, `kb query` stays behind its trigger.
- Two 070 entries filed: `task-priority-frontmatter` (med-high, long
  term), `task-synthesis-drift-check`. Why-not footnotes added to
  `class-task.md`; `evaluate-uses-independently` principle
  strengthened with the T3 unbundling instances; v1
  `llm-subtask/SKILL.md`'s `## Later` sentence corrected to the
  contract-boundary reading.
- sessions.kb normalization scheduled in
  `penguin/reconcile-sessions-kb-schema-drift.md`: dated-prefix
  rename sweep + taskfile retirement (operator-ratified; taskfiles
  obsolete between the task class and sessions.kb).
  `settle-sessions-kb-conventions.md` closed and deleted (#1–#3
  verified, #4 pursued);
  `CLAUDE.integrate-sessions-kb-into-llm-subtask.Task.md` deleted as
  obsoleted by T2+T3, residue parked as a T4 input line.
- Uncommitted at time of writing across three repos:
  bukzor-agent-skills, bukzor.claude-sessions, ~/.claude (dotfiles).

